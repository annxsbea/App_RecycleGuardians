import { View } from "react-native";
import { Text } from "react-native-paper";

export default function SignUp() {
    return (
        <View>
            <Text>SignUp</Text>
        </View>
    );
}