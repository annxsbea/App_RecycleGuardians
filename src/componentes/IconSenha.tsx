import React from "react";
import { Image } from "react-native";

export default function IconSenha() {
    return (
      <Image source={require('../assets/IconSenha.png')} style={{     position: 'absolute', top: 35, zIndex: 1,left: 15  }} />
   
    );
}